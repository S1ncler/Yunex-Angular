class Horario {
    valor: string;
    disabled: boolean;

    constructor(valor: string, disabled: boolean){
        this.valor = valor;
        this.disabled = disabled;
    }
}

export default Horario;